# Changelog

本文件面向使用者（用户/部署者）概括能力与风险变化；逐条工程明细见
`git log`。版本号遵循 [SemVer 2.0.0](https://semver.org/lang/zh-CN/)，
唯一权威源是仓库根的 `VERSION`（`scripts/check-version.py` 校验派生）。

## Unreleased（0.2.0 候选 — 未发布，无 tag）

发布前状态：候选。下方内容在最终验收通过前可能调整；本文不声称
"已上线"。

### 新增

- 邀请制多账户：一个运营者可给少量朋友开通独立账号。每个账号拥有
  独立的订阅、阅读状态、资料库、AI 配置与数据；账号由管理员发出
  一次性限时邀请、由受邀者自设用户名与密码激活（O145-O151）。
- 每账号独立 FreshRSS 绑定：RSS 订阅/已读/收藏的真源按账号隔离，
  支持运营者预建账号池（部署侧 CLI 准备，BFF 原子分配，无共享凭据
  回退）（O154/O155）。
- 管理员 API 与界面：成员生命周期（暂停/恢复/撤销会话/重置密码）、
  邀请管理、FreshRSS 池状态、最小审计记录（O146/O151/O152/O172）。
- 旧单用户数据自动迁移为 owner 账号：启动时幂等完成（文件级移动 +
  一致性备份），机器通道（订阅/投递 token）自动索引（O148）。
- 移动端阅读改造：导航抽屉修复（此前在部分场景无法打开）、阅读页
  内容优先重排（打开即正文，工具面板移至正文之后）、浮层历史与
  快速开关竞争修复（O121-O127）。
- 版本单一源：`VERSION` 文件 + 漂移检查（Web/BFF/镜像同源）
  （O190）；Conventional Commits 检查接入（O189）。
- Agent 任务运维：任务暂停与续接（已执行步骤绝不重复执行；暂停期间
  过期的批准在续接时要求重新确认）、线程级任务预算上限（预算耗尽给出
  消耗摘要，token 用量未上报时如实显示 unknown）、工具执行时间线
  （真实耗时 + 脱敏参数摘要）、批准前修改参数（旧批准作废）、失败步骤
  单独重试（成功写经幂等键防重复）、工作区/标签类写入的差异撤销（对象
  被改动过时冲突跳过）、可复用任务配方（工具白名单服务端强制执行）
  （N164–N170）。

### 变更

- BFF 数据层拆分：控制库（身份/会话/邀请）与每用户业务库分离；
  全部私有表按服务端验证身份路由，前端声明的 user_id 不再参与
  授权（O153/O158）。
- 全实例备份/恢复收归管理员操作；Obsidian 库只归 owner（账号/Agent
  工具均不可经成员身份触达）（O167/O168）。

### 升级与回退说明（候选部署必读）

- **迁移不可逆地改变数据布局**：升级后原 `lumi.sqlite` 成为控制库，
  业务数据位于 `data/users/<owner-id>/lumi.sqlite`。回退到旧版本
  需恢复升级前的备份（`./lumirss backup` 产物），不能简单切回旧镜像。
- 多账户面向小规模邀请制部署；公开注册默认不存在。对外开放前必须
  完成：所有账号改掉弱测试口令、FreshRSS 账号池就绪、反向代理
  TLS/限流就绪、备份策略演练。
- 运行期 AI 仍使用用户自有的 Provider 凭据；开发订阅不可作为多人
  推理后端。
